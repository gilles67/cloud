# Generated File sql/mysql_relay_domains.cf
user = {{ features.mail.name | default('mail') | lower }}
password = {{ features.mail.database.user_password }}
hosts = {{ features.mail.name | default('mail') | lower }}.{{ features.mail.database.vendor }}
dbname = {{ features.mail.name | default('mail') | lower }}
query = SELECT domain FROM domain WHERE domain='%s' AND active = '1' AND (transport LIKE 'smtp%%' OR transport LIKE 'relay%%')
